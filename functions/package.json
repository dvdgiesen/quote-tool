{
  "name": "watsturen-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for watsturen.nl",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "lint": "eslint --ext .js,.ts ."
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.0.0",
    "mammoth": "^1.8.0",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "~5.9.2"
  },
  "private": true
}
